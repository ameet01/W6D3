<h1>Welcome <%= current_user.username %>!</h1>

<%= render 'tweets/form', tweets_ul: '#feed' %>

<b>Your Feed!</b>

<div class="infinite-tweets">
  <ul id="feed">
    <% @feed_tweets.each do |tweet| %>
      <li><%= render 'tweets/tweet', tweet: tweet %></li>
    <% end %>
  </ul>
  <a href="#" class='fetch-more'>Fetch More</a>
</div>


